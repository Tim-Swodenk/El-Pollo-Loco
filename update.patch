diff --git a/models/drawable-object.class.js b/models/drawable-object.class.js
index 3e63f1bbdea4fa5a2da086013a325a16319f6251..53bc667ce348909647b1ec24a0acbd24f7e0f296 100644
--- a/models/drawable-object.class.js
+++ b/models/drawable-object.class.js
@@ -38,31 +38,31 @@ class DrawableObject {
     if (
       (this.offset && this instanceof Chicken) ||
       this instanceof Character ||
       this instanceof Endboss
     ) {
       ctx.beginPath();
       ctx.lineWidth = "2";
       ctx.strokeStyle = "red";
       ctx.rect(
         this.x + this.offset.left,
         this.y + this.offset.top,
         this.width - this.offset.left - this.offset.right,
         this.height - this.offset.top - this.offset.bottom
       );
       ctx.stroke();
     }
   }
 
   /**
    * Preloads multiple images and stores them in the cache.
    * @param {string[]} arr - Array of image paths.
    * @returns {void}
    */
   loadImages(arr) {
     arr.forEach((path) => {
-      let img = new Image();
-      img.src = path;
-      this.imageCache[path] = img;
+      let image = new Image();
+      image.src = path;
+      this.imageCache[path] = image;
     });
   }
 }
